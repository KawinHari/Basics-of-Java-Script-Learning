function checkspeed(speed)
{
    const limit =70;
    const point=5;
    if (speed < limit+point)
        console.log('ok');
    else{
        const points = Math.floor((speed-limit)/point);
        if (points>12)
            console.log('license is suspended');
        else
        {
            console.log('points ',points);
        }
    }
}

checkspeed(150)
